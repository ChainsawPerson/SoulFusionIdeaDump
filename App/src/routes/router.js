const baseURL = '/SoulFusion';

const databaseConfig = {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'soulfusion'
};

module.exports = {
    baseUrl : baseURL,
    databaseConfig : databaseConfig
};